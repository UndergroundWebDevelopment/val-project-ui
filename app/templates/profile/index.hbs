<div class="row">
    <div class="box">
        <div class="col-lg-12">
          {{#if model.dateOfBirth}}
            <h4>Birthdate: {{model.dateOfBirth}}</h4>
          {{/if}}
          {{md-text text=model.publicDescription}}
        </div>
    </div>
</div>
{{outlet}}
